export function buildParentEmailSummary(data: any) {
  const growthText = data.growthPoints == null ? "No previous test is available yet for a growth comparison." : data.growthPoints >= 0 ? `${data.studentName} improved by ${data.growthPoints} points since the previous assessment.` : `${data.studentName} scored ${Math.abs(data.growthPoints)} points lower than the previous assessment.`;
  return `<div style="font-family: Arial, sans-serif; color: #0f172a; line-height: 1.5;"><h1>Student Progress Summary</h1><p>Assessment: <strong>${data.assessmentTitle}</strong></p><p><strong>Student:</strong> ${data.studentName}</p><p><strong>Score:</strong> ${data.score ?? "N/A"}%</p><p><strong>Performance Level:</strong> ${data.performanceBand ?? "N/A"}</p><p><strong>Growth:</strong> ${growthText}</p><h2>Areas of Strength</h2><ul>${(data.strongestStandards || []).map((s: any) => `<li><strong>${s.standardCode}</strong> — ${s.standardLabel} (${s.percentScore}%, ${s.performanceBand})</li>`).join("")}</ul><h2>Areas to Practice</h2><ul>${(data.needsSupportStandards || []).map((s: any) => `<li><strong>${s.standardCode}</strong> — ${s.standardLabel} (${s.percentScore}%, ${s.performanceBand})</li>`).join("")}</ul></div>`;
}
