node_modules/
.next/
.env
.DS_Store
prisma/dev.db
tsconfig.tsbuildinfo
.vercel
.env*
# Regenerable v2 content pipeline build artifacts.
# Produced by scripts/phonogram/ and can run to many tens of MB.
# Re-generate locally with: python -m scripts.phonogram.build_*
.cache/phonogram/
data/phonogram/*.json
data/phonogram/*.csv
data/phonogram/*.sqlite
data/phonogram/*.md

# v1.4 diagnostic bank import staging — content lives in DB after import
content/reading-buddy-phase1-3-v1.4-fast-track/

# Local session audit outputs — regenerable via scripts/content/audit-*
audit/passage_audit_report.*
