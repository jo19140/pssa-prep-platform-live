"use client";
import { parentFriendlyGrowth, parentFriendlyPerformanceLevel } from "@/lib/parentFriendlyText";

export function StudentReport({ report }: { report: any }) {
  return <div className="space-y-6"><div className="rounded-3xl bg-emerald-700 p-6 text-white shadow"><h2 className="text-3xl font-bold">{report.student.name}</h2><p className="mt-2">{report.assessment.name}</p><div className="mt-4 grid gap-4 md:grid-cols-4"><Metric title="Score" value={`${report.summary.score}%`} /><Metric title="Performance" value={report.summary.performance} /><Metric title="Growth" value={report.summary.growthLabel || "N/A"} /><Metric title="Time" value={report.summary.timeOnTestLabel} /></div></div><div className="rounded-3xl bg-white p-6 shadow"><h3 className="text-xl font-bold">Growth Summary</h3><p className="mt-4 text-slate-700">{parentFriendlyGrowth(report.growth?.growthPoints ?? null)}</p></div><div className="rounded-3xl bg-white p-6 shadow"><h3 className="text-xl font-bold">Standards Mastery</h3><div className="mt-5 overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="text-slate-500"><tr><th className="pb-3 pr-4">Standard</th><th className="pb-3 pr-4">Description</th><th className="pb-3 pr-4">Score</th><th className="pb-3 pr-4">Band</th></tr></thead><tbody>{(report.standardsMastery || []).map((row: any) => <tr key={row.standardCode} className="border-t border-slate-100"><td className="py-3 pr-4 font-semibold">{row.standardCode}</td><td className="py-3 pr-4">{row.standardLabel}</td><td className="py-3 pr-4">{row.percentScore}%</td><td className="py-3 pr-4">{row.performanceBand}</td></tr>)}</tbody></table></div></div><div className="rounded-3xl bg-white p-6 shadow"><h3 className="text-xl font-bold">Question Review</h3><div className="mt-5 grid gap-4 lg:grid-cols-2">{(report.questionReview || []).map((item: any) => <div key={item.questionId} className="rounded-2xl border border-slate-200 p-4"><div className="text-xs font-semibold uppercase tracking-wide text-slate-500">{item.skill} • {item.questionType}</div><p className="mt-2 font-semibold">{item.question}</p><p className={`mt-2 text-sm font-semibold ${item.isCorrect ? "text-emerald-700" : "text-rose-700"}`}>{item.isCorrect ? "Correct" : "Incorrect"}</p><p className="mt-1 text-sm text-slate-600">Your answer: {item.studentAnswerLabel}</p><p className="mt-1 text-sm text-slate-600">Correct answer: {item.correctAnswerLabel}</p></div>)}</div></div></div>;
}
function Metric({ title, value }: { title: string; value: string }) { return <div className="rounded-2xl bg-white/15 p-3"><div className="text-xs uppercase tracking-wide text-emerald-100">{title}</div><div className="mt-1 text-xl font-bold text-white">{value}</div></div>; }
